export enum AuthStatus {
  AUTH = 'AUTH',
  NOT_AUTH = 'NOT_AUTH',
  UNKNOWN = 'UNKNOWN',
}

export enum AppRouteList {
  MAIN = '/',
  OFFER = '/offer',
  FAVORITES = '/favorites',
  LOGIN = '/login',
  NOT_FOUND = '*',
}
